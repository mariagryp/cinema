import { Title } from "../../components/Title";
import style from "./MoviePage.module.scss";

export const MoviePage = (p) => {
  return <div className={style.MoviePage}>
    <Title></Title>
  </div>;
};
